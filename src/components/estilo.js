import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  txtG: {
    fontSize: 24,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    textAlign: 'center',
    color: 'black',
  },
});
